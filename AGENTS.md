# 项目级 Multi-Agent 工作流

## 授权与触发条件

本项目授权 Root / Lead 按任务复杂度使用 Codex 原生 Multi-Agent；不要求用户每次额外输入开启口令。授权来自本项目规则，不得根据当前模型名称、model slug 或模型自我认知判断是否有权委派。

不要为了 Multi-Agent 而 Multi-Agent：

- 简单、局部、低风险、路径明确的任务由 Lead 直接完成。
- 只有需要跨文件或跨目录的大范围侦察、调用链定位、巨型非奠基性文件读取、日志与失败路径整理或独立事实核验时，才使用 Scout。
- 只有修改非平凡、风险较高、影响面较广，或独立审查能显著提高正确性时，才使用 Reviewer。
- 派发、等待和复核成本不低于直接处理时，不使用子代理。

## Root / Lead 职责

- Root / Lead 的委派权来自本项目工作流授权，而非模型名称、model slug 或模型自我认知；具体主模型由当前运行时配置决定。
- Lead 负责理解需求和验收标准，亲自阅读架构文档、设计文档、交接文档等奠基性材料。
- Lead 亲自阅读即将修改的确切代码，制定方案、作出技术和范围决策并实际修改代码。
- 强依赖的 A→B→C 编码工作由 Lead 顺序完成，不强行拆给多个 Worker。
- Lead 查看真实 Diff，运行或检查最终验证，判断何时达到验收条件并停止。
- Scout 和 Reviewer 只提供证据与审查意见；最终判断、代码修改和验收不得外包。

## 允许的常驻角色

只使用以下命名角色：

- `scout`：Luna Medium，只读侦察。
- `reviewer`：Terra High，只读独立审查。

不引入常驻 Worker、Tester、Architect、Debugger 或其他角色。并发子代理上限为 3，但上限不是派发目标。

## 派发纪律

- 每个任务必须自包含，写明检索或审查范围、具体问题、期望输出和停止条件。
- 派发 `scout` 或 `reviewer` 时使用 `fork_turns = "none"`，不复制完整主线程历史。
- 使用命名 `agent_type` 选择角色；不要在派发时覆盖角色文件固定的 model 或 reasoning effort。
- 多个相互独立且确有价值的侦察任务可在同一轮并发派发。
- 派发后 Lead 不重复执行已经委派的完整搜索；只能根据返回的 `file:line`、符号名和关键证据做定点抽查。
- 仍有会影响当前决策的子代理运行时，应等待其结果；明显超过 10 分钟时检查已有结果并缩小任务，而不是盲等或原样重试。
- Scout 和 Reviewer 都是 leaf agent，禁止继续派生子代理。

## Scout 使用规则

- Scout 用于宽而重的读取、定位和事实核验，不修改文件、不决定方案、不扩大范围。
- Scout 原则上 one-shot；优先一次性自包含派发，不把它复用成长生命周期线程。
- 只有补充问题与原任务紧密相关、复用其已建立的局部上下文明显比新派发更有价值时，才允许使用 `followup_task`。
- Scout 报告必须区分事实、推断、未确认项和未覆盖范围，并尽可能提供精确 `file:line`、符号名、配置键、参数、取值和必要原文。

## Reviewer 使用规则

- Reviewer 在 Lead 完成实际 Diff 后工作，重点检查 bug、边界条件、回归风险、API 与数据结构兼容性及测试遗漏。
- Reviewer 保持 read-only；可以运行已有测试和验证命令，但不得修改生产代码、已有测试或实现。
- 如果测试不足，Reviewer 只列出应补充的具体测试场景，由 Lead 实现。
- 如果正常测试因 read-only sandbox 无法生成构建产物，记录准确限制，后续再单独评估是否需要 workspace-write。
- 对重要修复，可使用 `followup_task` 唤醒同一个 Reviewer，复审修复后的真实 Diff。
- Reviewer 的结论不是最终验收；Lead 必须亲自检查关键证据和最终验证结果。

# Linux 生产部署与运维

- Core、Adapter 和 NapCat 的生产版本必须人工、独立且可追溯；禁止无人值守更新和浮动版本。生产服务器不得直接从远程代码托管站点拉取源码，源码更新必须经过受控、可校验的离线传输。Canonical 源码工作树保持干净，运行数据和用户 overrides 不得写入源码工作树。

- 生产版本切换必须保持可恢复性：切换前使用项目既有的一致性备份流程保护持久化数据；活动 SQLite 数据不得仅复制主数据库文件作为备份。新版本只能先作为 candidate 部署并经过实际运行验收，随后才能 promote 为 current。若版本变化可能改变持久化数据，回滚必须恢复兼容的数据状态，禁止把仅切回旧镜像视为完整回滚。

- 保持生产网络与秘密边界：管理 WebUI 仅允许 loopback/受控隧道访问，内部机器人接口不得发布到公网。Secret 只能存放在受控私有文件并在运行时读取，不得进入源码、命令参数、systemd unit、状态文件、日志或报告；对外展示生产日志必须先经过批准的脱敏流程，并限制为最小必要范围。

- 生产故障默认先只读诊断。重启服务或主机、修改 SSH/防火墙/云安全组、数据修复、破坏性清理和其他有状态生产变更，必须得到当前任务的明确授权；不得把“可能有助于恢复”视为授权。

- 生产服务器只作为运行环境，不作为 Codex/AI 开发环境；不得在服务器安装 Codex，也不得把本地 AI/OpenAI 配置、凭据或 SSH 私钥复制到服务器。

# 《杖剑传说》MaiBot 插件项目规则

## MaiBot Core 硬边界

- **绝对禁止修改 MaiBot Core。**
- MaiBot Core 在本项目中始终视为只读依赖，不属于本项目的开发区域。
- 所有功能必须通过 MaiBot 插件机制、当前 Plugin SDK、公开接口或插件自身代码实现。
- 如果某项需求在不修改 MaiBot Core 的前提下无法可靠实现，应停止该实现并明确说明限制，不得通过修改 Core 绕过。
- 不得为了方便、性能、兼容、减少插件代码或增加项目专用钩子而向 MaiBot Core 写入任何项目特有逻辑或补丁。

## MaiBot 集成

- 使用当前 MaiBot Plugin SDK 和 `self.ctx.*` 能力实现插件功能。
- 以当前 MaiBot 仓库和当前 `maibot_sdk` 实现为事实来源；外部插件和旧代码只能作为参考。
- 优先复用 MaiBot 已有的插件生命周期、消息发送、聊天流、配置和持久化能力，不重复实现框架已经提供的基础设施。
- 插件代码、配置、业务数据和运行状态应尽量自包含。
- 插件创建的后台任务、连接和其他运行时资源，必须在卸载或重载时正确释放。
- MaiBot SDK/API 发生变化时，应优先适配当前接口，不保留无实际需求的旧接口兼容层。

## 架构原则

- 功能按清晰职责拆分，但结构复杂度必须与实际需求匹配。
- 业务数据、业务规则、时间/进度计算、触发逻辑和 MaiBot 消息投递应尽量解耦，避免相互硬编码。
- 新功能优先复用现有模块，不为每个功能重新建立一套独立框架。
- 不为了潜在需求提前引入复杂抽象；只有真实功能出现稳定共性后，再提取公共层。
- 容易变化的游戏内容和外部数据优先数据驱动，不写死在核心业务代码中。

## 游戏数据

- 游戏数据与程序逻辑分离。
- 副本、活动、奖励、时间线、准入战力等游戏内容，应优先维护在结构化数据文件或明确的数据访问层中。
- 修改游戏内容通常不应要求修改核心业务逻辑。
- 重要数据必须能够区分可靠程度，例如正式服确认、部分确认、测试服、预测或待确认。
- 未确认、预测、测试服数据不得默认作为确定事实向用户展示。
- 缺失数据不得由代码、模型或开发者自行猜测补全。
- 如果未来引入外部 API、自动同步或抓取，必须保留明确的数据来源、错误处理和必要的回退能力；不得让核心功能无保护地依赖不稳定第三方服务。

## 时间与服务器进度

- 所有“开服第 N 天”“赛季第 N 天”等计算必须明确 Day 1 定义，并集中处理日期边界，避免 off-by-one。
- 不假设所有赛季、所有服务器永久共享同一绝对开服时间线。
- 数据模型必须允许服务器进度、赛季锚点或时间偏移存在差异。
- 时间和服务器进度计算逻辑应集中管理，不允许不同功能各自实现一套日期算法。

## 消息与通知

- 确定性的游戏数据通知优先生成确定性内容，不为了文本生成而调用 LLM。
- 主动消息通过 MaiBot 当前支持的稳定目标身份解析和消息发送能力完成。
- 需要防重复的功能必须具有跨 MaiBot 重启有效的持久化状态。
- 只有确认消息发送成功后，才能把对应通知记录为已完成。
- 避免因重启、配置热更新或短暂发送失败造成重复刷群。

## 配置与持久化

- 用户可调整的运行参数与游戏数据分离。
- 配置只保存真正需要由使用者修改的内容，不把大量内部实现细节暴露成配置项。
- 插件运行数据使用 MaiBot 提供的插件数据目录或当前推荐的持久化机制，不写入源码目录。
- 只有当现有简单存储已经不能可靠满足需求时，才引入数据库等更复杂的持久化方案。
- 持久化格式发生变化时，应考虑已有数据的兼容或迁移。

## 功能扩展

- 本项目会根据公会的真实使用需求持续增加功能，不把项目永久限定为单一通知插件。
- 新功能首先判断它属于新数据、新业务规则、新用户交互、新外部数据源还是新的 MaiBot 集成能力，再在对应层扩展。
- 不因新增一个功能而重构无关模块。
- 如果新增功能暴露出现有架构的真实重复或耦合，再进行针对性重构。
- 不因假设未来可能公开发布、支持大量服务器或复杂运营场景，而提前建设当前没有需求的通用平台。
- 本文件只描述长期项目规则，不记录某个版本临时“做什么 / 不做什么”的功能清单。

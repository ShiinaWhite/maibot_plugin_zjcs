# Design Decisions

本文记录当前已经生效的设计决策及理由。

不记录已经否决的方案。

## 1. MaiBot Core 永远只读

决定：

本项目绝不修改 MaiBot Core。

原因：

- 插件应保持独立；
- 避免 MaiBot 升级、迁移、回滚被项目补丁绑架；
- 项目专用逻辑不应该进入 Core；
- 如果插件 API 无法支持某功能，应明确暴露限制，而不是偷偷修改宿主。

## 2. 项目不永久限定为“通知脚本”

决定：

第一阶段先做服务器进度通知，但长期项目仍然是《杖剑传说》MaiBot 公会功能插件。

原因：

后续很可能继续增加查询、数据展示或其他公会实用功能。

因此长期 `AGENTS.md` 不写 V1 专属功能边界。

## 3. V1 使用本地静态时间线

决定：

运行时直接读取本地结构化数据，不依赖外部 API。

原因：

- 当前没有找到稳定公开的官方国服 API；
- 社区工具本身也大量依赖整理数据；
- 对通知类功能而言，本地数据更稳定、更容易验证；
- 当前服务器仍在 S2，不需要为了远期数据自动化延迟施工。

未来若出现可靠 API，可再增加同步层。

## 4. 每日扫描，而不是每事件一个长期定时器

决定：

V1 只维护一个每日检查任务。

原因：

- 事件数量有限；
- 时间线数据会继续更新；
- 修改 JSON 后可以直接生效；
- 不需要维护大量跨数周/数月的 sleep / timer；
- 重启恢复逻辑更简单。

## 5. 确定性通知不经过 LLM

决定：

副本、活动、奖励和日期通知直接生成固定文本。

原因：

- 数据本身是确定性的；
- 不需要额外 Token；
- 避免模型改写数值、日期或奖励；
- 输出更稳定、易测试。

LLM 是否用于未来其他功能，届时再单独设计。

## 6. 防重复只做最小持久化

决定：

第一阶段记录最小成功通知键，例如：

```text
(event_id, event_date, remind_days_before)
```

原因：

当前只服务一个公会服务器，没有必要先建设完整投递日志数据库。

只有在真实需求出现后，再升级存储模型。

事件日期属于去重键的一部分；时间线调整日期后，应允许新日期重新触发提醒。

## 7. 后期赛季允许独立锚点

决定：

不能把所有未来赛季永久写死成唯一绝对开服 Day。

原因：

公开资料已经观察到位面流通 / 合区等造成的整体时间偏移。

因此后续时间模型允许：

```text
season_anchor_date + season_day
```

## 8. 项目文档分层

长期采用：

- `AGENTS.md`：长期项目规则；
- `PROJECT_BRIEF.md`：长期项目背景；
- `PROJECT_STATUS.md`：当前阶段状态；
- 当前任务文件：某一阶段具体施工范围。

原因：

避免把 V1 / V2 / V3 临时需求不断写进长期项目规则，也避免后来进入项目的会话缺少必要背景。

## 9. 项目协作与 Review 交接

当前项目主聊天承担总设计和阶段决策，负责功能方向、产品需求、数据策略、长期架构、
阶段复盘和下一阶段设计。

日常函数级施工、测试失败和小型修复留在 Codex 会话中。每个明显施工阶段完成后，
通过 GitHub 的 commit 和 diff 交给人工或外部 GPT Review，并将真实状态同步回项目文档。

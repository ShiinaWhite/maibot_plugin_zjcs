# Design Decisions

本文记录当前已经生效的设计决策及理由。

不记录已经否决的方案。

## 1. MaiBot Core 永远只读

决定：

本项目绝不修改 MaiBot Core。

原因：

- 插件应保持独立；
- 避免 MaiBot 升级、迁移、回滚被项目补丁绑架；
- 项目专用逻辑不应该进入 Core；
- 如果插件 API 无法支持某功能，应明确暴露限制，而不是偷偷修改宿主。

## 2. 项目不永久限定为“通知脚本”

决定：

第一阶段先做服务器进度通知，但长期项目仍然是《杖剑传说》MaiBot 公会功能插件。

原因：

后续很可能继续增加查询、数据展示或其他公会实用功能。

因此长期 `AGENTS.md` 不写 V1 专属功能边界。

## 3. 使用本地静态时间线

决定：

运行时直接读取本地结构化数据，不依赖外部 API。

原因：

- 当前没有找到稳定公开的官方国服 API；
- 社区工具本身也大量依赖整理数据；
- 对通知类功能而言，本地数据更稳定、更容易验证；
- 决策时服务器仍处于早中期赛季，不需要为了远期数据自动化延迟施工。

未来若出现可靠 API，可再增加同步层。

## 4. 每日扫描，而不是每事件一个长期定时器

决定：

项目只维护一个每日检查任务。

原因：

- 事件数量有限；
- 时间线数据会继续更新；
- 修改 JSON 后可以直接生效；
- 不需要维护大量跨数周/数月的 sleep / timer；
- 重启恢复逻辑更简单。

## 5. 确定性通知不经过 LLM

决定：

副本、活动、奖励和日期通知直接生成固定文本。

原因：

- 数据本身是确定性的；
- 不需要额外 Token；
- 避免模型改写数值、日期或奖励；
- 输出更稳定、易测试。

LLM 是否用于未来其他功能，届时再单独设计。

## 6. 通知去重：业务键保持稳定，状态按群隔离

决定：

业务 notification key 保持最小形式：

```text
event_id:event_date:remind_days_before
```

持久化状态按群隔离：

```text
group_id -> sent keys
```

notification state schema 为 version 2（`{"version": 2, "groups": {...}}`）。

原因：

- 多目标群之间发送成功 / 失败必须相互独立；
- A 群发送成功、B 群失败时，重启后只补发 B，不会让 B 永远收不到，也不会让 A 重复收到；
- group 是状态的 namespace，不需要把 `group_id` 拼进业务 key 文本本身；
- 事件日期属于去重键的一部分；时间线调整日期后，新日期会形成新 key 重新触发提醒。

## 7. 每类仅一次提醒，0 为关闭

决定：

每种内容类别只使用一个提前提醒天数（单整数），`0` 表示关闭该类别。

不使用同一类别 `[2, 1]` 这类多次提醒列表轰炸同一周期。

原因：

- 每类内容在“提前准备”语义下只需要一次提醒；
- 不同类别可根据准备需求使用不同提前时间（例如宾果提前 4 天积攒果子，副本提前 1 天）；
- 单整数让配置、校验和迁移都保持简单。

## 8. 同一群同一检查批次合并消息

决定：

每日检查命中的多个提醒合并为一条消息发送；同一群在一次每日检查中最多收到一条合并消息。

多群场景下，每个群根据自己的去重状态生成自己的 merged body。

原因：

- 避免同一群一天内收到多条刷屏消息；
- 部分失败补发时，群收到的正文只包含它缺失的内容，不能把所有群的 pending 并集正文直接共享给每个群（否则已收到过的群会看到重复条目）。

## 9. 后期赛季允许独立锚点

决定：

不能把所有未来赛季永久写死成唯一绝对开服 Day。

原因：

公开资料已经观察到位面流通 / 合区等造成的整体时间偏移。

因此后续时间模型允许：

```text
season_anchor_date + season_day
```

## 10. 项目文档分层

长期采用：

- `AGENTS.md`：长期项目规则；
- `PROJECT_BRIEF.md`：长期项目背景；
- `PROJECT_STATUS.md`：当前阶段状态；
- `DEPLOYMENT.md`：生产部署事实与操作规则；
- `docs/archive/`：已完成阶段的历史任务文件（带归档标记，不删除）。

原因：

避免把各阶段临时需求不断写进长期项目规则，也避免后来进入项目的会话缺少必要背景，或把已过时的部署认知当成现状。

## 11. 项目协作与 Review 交接

项目主聊天 / 设计层负责需求、架构、产品方向、数据策略、阶段验收和下一阶段设计。

实现模型 / 开发环境负责函数级代码施工、测试实现和小型修复。具体使用哪个模型或工具由当时的运行环境决定，流程本身不绑定特定模型。

每个明显施工阶段完成后，必须基于真实 Git commit / diff 做独立 Review；不信任仅有文字总结的“已验证”。

# Project Brief

## 项目是什么

这是一个基于 MaiBot 的《杖剑传说》公会功能插件项目。

项目最初从“服务器进度通知”开始，但不会永久限制为单一通知功能。后续会根据公会的真实使用需求逐步增加其他实用功能。

当前主要使用场景是单个公会自己的 QQ 群，不以立即公开发布或做成大型通用平台为目标。

## 当前起点

项目的第一个功能是根据服务器时间线自动提醒：

- 下一个副本什么时候开放；
- 下一个活动什么时候开启或换期；
- 下一个重要服务器事件什么时候发生；
- 副本通知附带已确认的准入战力；
- 秘宝大作战通知附带当期最有价值的重点奖励。

通知的核心价值不是单纯报日期，而是让公会成员提前知道“接下来要准备什么”。

## 当前数据资产

已经准备了第一版结构化数据：

- `timeline_v1.json`

当前包含：

- 副本开放时间；
- 各难度准入战力；
- 秘宝大作战期数与重点大奖；
- 周期活动；
- 新国度、赛季地图、遗物池等重要事件；
- 数据来源与可信度状态。

公开资料调研结论是：目前没有发现稳定、公开、官方支持的国服《杖剑传说》通用 API。已有玩家工具和小程序很大程度上依赖社区整理、固定时间线、玩家反馈以及部分客户端数据。因此第一阶段优先使用本地结构化数据，不让运行时依赖未知第三方接口。

## 长期产品方向

这个项目优先解决公会真正遇到的问题。

未来可能扩展的方向包括但不限于：

- 群内查询下一个副本 / 活动；
- 查询服务器当前赛季进度；
- 查询秘宝奖励；
- 更多副本或赛季数据；
- 更方便的配置方式；
- 如果未来出现可靠数据源，再评估自动同步。

这些都不是当前版本的默认承诺，应在真实需求出现后逐步设计。

## 技术边界

MaiBot Core 永远是只读依赖。

项目的功能必须通过 MaiBot 插件能力实现，不允许为了本插件修改 MaiBot Core。

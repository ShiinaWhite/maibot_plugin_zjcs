# Current Task — V1 服务器进度通知

## 目标

实现《杖剑传说》公会插件的第一个可用功能：

根据当前服务器进度，每天检查本地时间线，并提前若干天向指定 QQ 群主动通知即将开放的副本、活动和重要事件。

V1 先解决一个真实、明确的问题，不追求把整个项目一次做完。

## 当前通知内容

### 副本

通知：

- 副本名称；
- 距离开放还有几天；
- 实际开放日期；
- 数据中已经确认的各难度准入战力。

例：

```text
【杖剑传说 · 副本提醒】

还有 2 天开放：

龙之国 · 青云观

普通：620 万
困难：770 万
噩梦：980 万

开放日期：XXXX-XX-XX
服务器进度：开服第 73 天
```

缺失难度不补猜。

### 秘宝大作战

通知：

- 第几期；
- 距离开启还有几天；
- 当期重点大奖。

例：

```text
【杖剑传说 · 活动提醒】

还有 2 天：

秘宝大作战 · 第 11 期

重点奖励：
自选 4 阶技能碎片 ×180

开放日期：XXXX-XX-XX
```

### 其他事件

包括已经录入时间线的：

- 新国度；
- 赛季地图；
- 遗物池；
- 其他重要服务器事件。

只需要简洁说明事件、日期和剩余天数。

## 建议运行方式

V1 使用**一个每日检查后台任务**。

不要为时间线中的每个未来事件创建一个睡眠几十天或几个月的独立定时器。

逻辑：

```text
每日检查触发
→ 计算服务器当前 Day / 赛季进度
→ 查找未来提醒窗口中的事件
→ 检查对应提醒是否已经成功发送
→ 生成固定通知文本
→ 根据 QQ group_id 解析 stream_id
→ 主动发送
→ 发送成功后记录防重复状态
```

这样修改 `timeline_v1.json` 后可以直接生效，不需要重新创建一批长期定时任务。

## 最小配置

第一版只需要当前公会使用：

- `enabled`
- 目标 QQ `group_id`
- 服务器开服日期
- 每日检查时间
- 提前提醒天数，例如 `[2, 1]`

可以为后续赛季保留必要的赛季 anchor / offset 数据能力，但不为它开发复杂后台或管理系统。

## 防重复

同一事件、同一事件日期、同一提前天数，只允许成功通知一次。

最小唯一键可类似：

```text
(event_id, event_date, remind_days_before)
```

例如：

```text
qingyun_temple:2026-09-04:2
qingyun_temple:2026-09-04:1
treasure_11:2026-09-06:2
```

状态必须跨 MaiBot 重启有效。

只有真正确认消息发送成功后才能写入“已发送”。

如果时间线调整了事件日期，新日期会形成新的唯一键，不会被旧日期的状态阻止重新提醒。

V1 优先使用插件自己的简单持久化状态；除非当前 MaiBot 已有更标准且同样简单的方案，否则不为了这一点引入完整数据库。

## 数据

运行时使用本地 `timeline_v1.json`。

开发过程中：

- 不重新联网收集《杖剑传说》数据；
- 不自行猜测缺失字段；
- `pending` 或未确认内容不作为确定信息发送；
- 游戏数据变化优先修改数据文件，不修改核心调度逻辑。

## V1 暂不实现

本阶段不做：

- LLM 问答；
- 攻略查询；
- 在线抓取游戏数据；
- 自动更新 timeline；
- 多服务器管理；
- Web 管理后台；
- 复杂权限系统；
- 完整消息投递日志平台；
- 为公开发布准备的大量通用抽象。

这些只是当前施工范围，不是项目永久禁区。

## 验收条件

1. 配置服务器开服日期、目标 QQ 群和提醒天数后，插件能在正确日期自动检查时间线。
2. 命中副本事件时能主动向指定 QQ 群发送消息。
3. 副本通知正确展示 JSON 中已确认的准入战力。
4. 秘宝大作战通知正确展示期数和重点奖励。
5. 普通活动和重要事件能够正确提醒。
6. 没有符合提醒条件的事件时不发送消息。
7. 同一事件、同一提前天数只成功发送一次。
8. MaiBot 重启后不会重新刷已成功发送的提醒。
9. 修改 `timeline_v1.json` 后无需修改核心业务代码。
10. 插件卸载或重载后不会遗留失控后台任务。
11. 全部功能通过插件实现，MaiBot Core 保持零修改。
